import React from 'react';

const Profile = () => {
    return (
        <div className="flex items-center justify-center h-screen bg-gray-100">
            <h2 className="text-3xl">Profile Page</h2>
            {/* Add profile details and editing options */}
        </div>
    );
};

export default Profile;
